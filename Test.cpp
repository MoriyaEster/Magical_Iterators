#include <stdio.h>
#include "doctest.h"
#include <stdexcept>
#include <sstream>
#include "iostream"

#include "sources/MagicalContainer.hpp"

using namespace ariel;

TEST_SUITE("MagicalContainer Tests")
{
    TEST_CASE("Test size after adding elements")
    {
        MagicalContainer container;
        CHECK(container.size() == 0);
        CHECK(container.size() == 0);
        CHECK(container.size() == 0);
        CHECK(container.size() == 0);
        CHECK(container.size() == 0);
        CHECK(container.size() == 0);
        CHECK(container.size() == 0);
        CHECK(container.size() == 0);
        CHECK(container.size() == 0);
        CHECK(container.size() == 0);
        CHECK(container.size() == 0);
        CHECK(container.size() == 0);
        CHECK(container.size() == 0);
        CHECK(container.size() == 0);
        CHECK(container.size() == 0);
        CHECK(container.size() == 0);
        CHECK(container.size() == 0);
        CHECK(container.size() == 0);
        CHECK(container.size() == 0);
        CHECK(container.size() == 0);

    }

}